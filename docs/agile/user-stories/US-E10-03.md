# User Story: US-E10-03 - ปุ่มปิด Popup วิดีโอระหว่างคลิปกำลังเล่น

**Status:** ✅ **Done** (v1.2.3, 2026-07-20) — **[Sprint 10](../sprint-backlogs/sprint-10.md)** (Active)
**Epic:** [E10: Sprint 10 Additions — Test Group Segmentation & UX](../01-product-backlog.md)
**Owner:** [Developer/AI]
**Source:** Owner request (2026-07-20) — งานใหม่ใน Sprint 10 ไม่ใช่ส่วนต่อขยายของงาน art ใน E7
**Priority:** 🟠 P1 — ผู้เล่นติดอยู่ในคลิปที่ยาวโดยไม่มีทางออก
**Target version:** ✅ `1.2.3` (**PATCH** — เติมทางออกให้ flow เดิม)

---

## 📖 Description
**ในฐานะ** ผู้เล่นสูงอายุที่กำลังดูละครสั้นประจำวัน
**ฉันต้องการ** ปิดหน้าต่างวิดีโอได้ระหว่างที่คลิปยังเล่นอยู่
**เพื่อให้** ไม่ต้องนั่งรอจนจบคลิปที่บางทีก็ยาวเกินไป กว่าปุ่ม "กลับไปหน้าเกม" จะโผล่มา

ตอนนี้ปุ่ม `กลับไปหน้าเกม` (`.gh-start-button`) จะแสดง **ก็ต่อเมื่อคลิปเล่นจบแล้ว** เท่านั้น
(`.gh-video-popup.is-ended .gh-video-popup__button { display: block }`) ระหว่างที่คลิปยังเล่น
ผู้เล่นจึงไม่มีปุ่มออกเลย

**ปุ่มทั้งสองต้องแสดงสลับกัน** — ปุ่มปิดโผล่ตอนกำลังเล่น, ปุ่ม `กลับไปหน้าเกม` โผล่ตอนจบ
ไม่แสดงพร้อมกัน

---

## 🔎 สิ่งที่พบตอนสำรวจโค้ด (อ่านก่อนเริ่มทำ)

**owner ใส่ปุ่มย้อนกลับไว้แล้ว (ยังไม่ commit) แต่ยังเป็นปุ่มตาย** — `checkin-summary-screen.js`
เรนเดอร์ `renderIconButtonBack()` ไว้ในแถบหัวเรื่องพร้อม CSS `.gh-video-popup__title-back*`
และเปลี่ยน label ปุ่มตอนจบจาก `ต่อไป` เป็น `กลับไปหน้าเกม` แล้ว ผู้เล่นเห็นปุ่มและกดได้
แต่ `wire()` ผูก event ให้เฉพาะ `.gh-start-button` เท่านั้น **ยังไม่มีใครผูก handler ให้ปุ่มนี้**
กดแล้วจึงไม่เกิดอะไรขึ้น

⚠️ ดังนั้นงานนี้ **ไม่ใช่การเพิ่มปุ่มใหม่** แต่คือการทำให้ปุ่มที่มีอยู่แล้วทำงาน + สลับการแสดงผลกับ
ปุ่มตอนจบ ซึ่งถูกกว่าและไม่ต้องเพิ่ม art ใหม่

⚠️ ปุ่มนั้นยังใช้ `id="player-info-back"` ซึ่ง**ก็อปมาจาก `player-info-screen.js:135`** ทั้งดวง —
เป็น id ที่สื่อความหมายผิด (ไม่ใช่หน้า player-info) และเสี่ยง id ซ้ำถ้าวันหนึ่งสอง DOM อยู่พร้อมกัน
ควรเปลี่ยนเป็น id ของตัวเอง เช่น `checkin-video-close`

---

## ✅ Acceptance Criteria
1. [x] ระหว่างคลิปกำลังเล่น ปุ่มย้อนกลับแสดงและกดได้จริง (ผูก handler แล้ว)
2. [x] คลิปจบ → ปุ่มย้อนกลับหาย ปุ่ม `กลับไปหน้าเกม` แสดงแทน; **กดเล่นซ้ำก็สลับกลับ** (`play` ถอด `is-ended`)
3. [x] กดปิดกลางคัน → popup ปิดและกลับ Game Hub เหมือนกดปุ่มตอนจบ
4. [x] `cleanup()` เรียก `destroy()` + `dismissPopup()` อยู่แล้ว — ไม่มีเสียงค้าง
5. [x] ทรานสิชั่นปิดตาม [US-E7-20](US-E7-20.md)
6. [x] `id` → `checkin-video-close`
7. [x] `aria-label` = "ปิดวิดีโอแล้วกลับไปหน้าเกม" และเป็น `<button>` จริง กดด้วยคีย์บอร์ดได้

---

## ❓ คำถามที่ค้างไว้ — เคลียร์แล้ว
- ~~ปิดกลางคันนับว่า "ดูคลิปแล้ว" หรือยัง?~~ → **ตกไป** เพราะตรวจแล้วพบว่าทั้ง `game-hub-screen.js`
  และ `player-info-screen.js` เรียก `await showCheckInPopup({...})` โดย**ไม่ได้อ่านค่าที่ resolve**
  เช็คอินไม่ได้ผูกกับการดูคลิปจบตั้งแต่แรก ปิดกลางคันจึงข้ามอะไรไม่ได้อยู่แล้ว
- **ใช้ `Icon_ButtonBack` (ลูกศร) ตามที่ owner ใส่ไว้** — ยังไม่ได้ทำไอคอนกากบาท ถ้าผู้ทดสอบสับสนว่า
  ลูกศรแปลว่า "ย้อนกลับ" ไม่ใช่ "ปิด" ค่อยขอ art เพิ่มทีหลัง

---

## 🛠 Technical Tasks
- [ ] ผูก handler ให้ปุ่มย้อนกลับใน `buildStep()` (`wire()`) ของ step `video` — เรียก `cleanup()` เส้นทางเดียวกับ `.gh-start-button`
- [ ] เพิ่ม CSS สลับการแสดงผล: ปุ่มปิดซ่อนเมื่อ `.gh-video-popup.is-ended` (ตรงข้ามกับ `.gh-video-popup__button` ที่มีอยู่)
- [ ] ตรวจว่า `cleanup()` ปลาย ๆ เรียก `state.videoPlayerInstance?.destroy()` ครบเมื่อปิดกลางคลิป (มีอยู่ที่บรรทัด ~280 — ยืนยันว่าเส้นทางนี้ผ่านจริง)
- [ ] เปลี่ยน id ปุ่มเป็น `checkin-video-close`
- [ ] ทดสอบ: ปิดกลางคลิป / ดูจนจบแล้วกดปุ่มเดิม / เปิดซ้ำวันเดียวกัน

---

## 🔗 Related Files
- Video popup: `src/ui/checkin-summary-screen.js` (step `video`, ~บรรทัด 434–480)
- CSS: `public/components.css` → `.gh-video-popup*` (~1758–1842)
- ปุ่ม: `src/ui/components/icon-button-back.js`, `src/ui/components/start-game-button.js`
- Player: `src/util/video-player/index.js`, `src/core/video-manager.js`
- ทรานสิชั่น: `src/ui/transition/popup-transition.js` ([US-E7-20](US-E7-20.md))
- Prior: [US-E7-11](US-E7-11.md) (ละครสั้น/วิดีโอ), [US-E7-16](US-E7-16.md) (popup หลังดูละคร), [US-E7-04](US-E7-04.md) (popup art)
- Backlog: [01-product-backlog](../01-product-backlog.md)
- Sprint: [Sprint 10](../sprint-backlogs/sprint-10.md) — Active

---
Back to Product Backlog: [Product Backlog](../01-product-backlog.md) | Back to Index: [Index](../../index.md)
