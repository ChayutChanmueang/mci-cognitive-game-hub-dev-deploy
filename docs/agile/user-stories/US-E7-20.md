# User Story: US-E7-20 - ระบบทรานสิชั่น: Popup fade+scale / เปลี่ยนหน้า fade + แยก Loading component

**Status:** ✅ Done (Sprint 7) — implemented v0.21.0; ยืนยันการแสดงผลถูกต้องโดยเจ้าของงาน (2026-07-02)
**Epic:** [E7: Game Art Assets & UI/UX Polish](../01-product-backlog.md)
**Owner:** ก้องไผ่
**Source:** Owner request (2026-07-02), [Sprint 07](../sprint-backlogs/sprint-07.md)

---

## 📖 Description
**ในฐานะ** ผู้เล่นสูงอายุ
**ฉันต้องการ** ให้ popup และการเปลี่ยนหน้ามีอนิเมชันนุ่มนวล (ค่อย ๆ ปรากฏ/หายไป ไม่กระโดด)
**เพื่อให้** การใช้งานลื่นไหล ไม่สะดุดตา และดูเป็นเกมที่เป็นมิตร

---

## 🎯 Requirements (2 ข้อ)

### Requirement 1 — อนิเมชัน Popup (fade + scale)
- popup **ทั้งหมดในเกม** ต้องมีอนิเมชัน **fade-in + ขยายขึ้น (scale up)** ตอนเปิด และ **fade-out + ย่อลง (scale down)** ตอนปิด
- ระยะเวลา fade + ย่อ/ขยาย = **0.5 วินาที**

### Requirement 2 — อนิเมชันเปลี่ยนหน้า (page transition)
- เพิ่ม **fade-in / fade-out ตอนเปลี่ยนหน้า** เช่น หน้าโลโก้เกม → หน้า Login → หน้าลงทะเบียน
- ยกเว้นหน้า **Game Hub และ minigame** ให้แสดง **Loading** แทน (ไม่ใช้ fade ธรรมดา) — ตัว loading overlay ถูกสร้างไว้แล้วใน `index.html` (`#app-loading`)
- **แยก loading overlay ออกจาก `index.html` มาเป็นไฟล์ component แยกใน `src/ui/`** เพื่อให้แก้ไข/นำกลับมาใช้ซ้ำได้ง่าย
- หน้าอื่น ๆ (นอกเหนือ Game Hub / minigame) ใช้ fade-in / fade-out ตามปกติ

---

## 🔎 สถานะปัจจุบัน (2026-07-02)
- **Popup:** popup ทั้งหมด (`popup-dialog.js`, `game-exit-popup.js`, `resting-point-popup.js`, `day-completion-popup.js`, `checkin-summary-screen.js`) สร้าง overlay `.app-popup` แล้ว `innerHTML` + `appendChild` โดย **ไม่มี** อนิเมชัน enter/leave — โผล่/หายทันที
- **เปลี่ยนหน้า:** `src/main.js` เปลี่ยนหน้าโดย `uiRoot.innerHTML = ""` แล้ว render ใหม่ทันที — ไม่มีทรานสิชั่น
- **Loading:** boot loading overlay เป็น HTML ฝังตายใน `index.html` (`#app-loading`, ใช้ `Logo.png` + `.dotted-loader`) และถูกถอดออกด้วย `finishBootLoading()` ใน `main.js` (เพิ่ม `.app-loading--hidden` แล้ว `remove()` ที่ 400ms); สไตล์อยู่ใน `public/style.css` (`#app-loading`) — **ยังไม่ได้** แยกเป็น component ใน `src/ui/`

---

## ✅ Acceptance Criteria
1. [x] popup ทุกตัวในเกมมี fade-in + scale-up ตอนเปิด และ fade-out + scale-down ตอนปิด (ระยะ **0.5s**), รองรับผ่านโครง `.app-popup` ที่ใช้ร่วมกัน
2. [x] อนิเมชันปิด popup เล่นจบก่อน แล้วจึงลบ element / resolve ผลลัพธ์ (ไม่ตัดจบทันที) — คง contract เดิมของแต่ละ popup (Promise true/false/value, Escape, backdrop, focus, scroll-lock)
3. [x] การเปลี่ยนหน้าปกติ (โลโก้ → Login → ลงทะเบียน ฯลฯ) มี fade-out หน้าเก่า → fade-in หน้าใหม่
4. [x] เข้า **Game Hub** และ **minigame** แสดง Loading แทน fade (ทุกครั้งที่เข้า)
5. [x] Loading overlay มี **component แยกใน `src/ui/loading-overlay.js`** (show/hide จากโค้ดได้) — โดย **คง** boot overlay แบบ inline ใน `index.html` ไว้เพื่อ paint ทันที (ดูหมายเหตุ "Loading อยู่สองที่" ด้านล่าง)
6. [x] เคารพ `prefers-reduced-motion: reduce` — ปิด/ลดอนิเมชัน (ทั้ง popup, page fade)
7. [x] ไม่ล้มพฤติกรรมเดิม (back-button guard, exit log, route-race — ดู [PB-01-01](../problems/PB-01-01.md)); ใช้ `routeRenderVersion` guard เดิม, loading ปิดใน `finally`
8. [x] ยืนยันการแสดงผล/จังหวะอนิเมชันโดยเจ้าของงาน (2026-07-02)

---

## ⚠️ หมายเหตุ: Loading อยู่ "สองที่" (สำคัญสำหรับคนแก้ต่อ)
มี loading screen **สองชุดโดยตั้งใจ** — หน้าตาเหมือนกัน แต่คนละบทบาท ถ้าจะแก้ต้องแก้ให้ตรงกันทั้งคู่:

1. **Boot overlay (inline)** — `index.html` (`#app-loading`) + critical inline style
   - จำเป็นต้อง inline เพราะต้อง **paint ทันทีตอนเปิดหน้า** ก่อน JS bundle/`style.css` มาถึง (กันจอขาวตอน cold start — BUG-005 / PB-01-01)
   - ถูก fade + ลบ **ครั้งเดียว** โดย `main.js#finishBootLoading()` เมื่อหน้าจอแรกพร้อม
2. **Component (JS)** — `src/ui/loading-overlay.js` (`.app-loading--component`)
   - ก๊อปปี้งานศิลป์เดียวกันในรูป JS เพื่อ **show/hide ได้ทุกครั้ง** ตอนเข้า Game Hub / minigame (ตอนนั้น boot overlay ถูกลบไปแล้ว ใช้ซ้ำไม่ได้)

**ทำไมต้องมีสองอัน:** boot overlay แลกความ "แก้ยาก" (ฝัง markup+style ใน HTML) กับการ paint ทันที ส่วน component แลกการ "แก้ง่าย/reuse" กับการที่มันโผล่ได้ก็ต่อเมื่อ JS โหลดแล้ว — เอา boot ออกทั้งหมดจะเกิดจอขาว จึงคงไว้ทั้งคู่
**สไตล์ที่ใช้ร่วมกัน:** `public/style.css` — `.app-loading__logo`, `.dotted-loader`, `@keyframes dot-*` (กล่อง/z-index ของ component อยู่ที่ `.app-loading--component`)
**ถ้าเปลี่ยนหน้าตา loading:** ต้องแก้ **ทั้ง** markup inline ใน `index.html` และ `build()` ใน `loading-overlay.js`

---

## 🛠 Technical Tasks
- [x] เพิ่ม CSS enter/leave ให้ `.app-popup` (fade + `transform: scale()`, 0.5s) + `@media (prefers-reduced-motion)` ใน `public/style.css`
- [x] เพิ่ม helper `dismissPopup()` (`src/ui/transition/popup-transition.js`) ที่รออนิเมชันปิดจบก่อน remove+resolve; ต่อเข้าทุก popup: `popup-dialog.js`, `game-exit-popup.js`, `resting-point-popup.js`, `day-completion-popup.js`, `checkin-summary-screen.js`
- [x] check-in popup แบบหลายหน้า (success→calendar→video): เปิด/ปิด popup ใช้ fade+scale เดิม แต่ **สลับหน้าใน popup ใช้ crossfade (opacity อย่างเดียว ไม่ย่อ/ขยาย) และไม่ fade พื้นหลัง** — โครง `.checkin-popup` (wrapper แอนิเมต) + `.checkin-popup__stage` (สลับเนื้อหา) + backdrop persistent
- [x] สร้าง `src/ui/transition/screen-transition.js` (`renderWithFade`) + CSS `#ui-root` fade; `main.js#renderCurrentRoute` ห่อ show* ของหน้า DOM ทั้งหมดด้วย `renderWithFade`
- [x] สร้าง `src/ui/loading-overlay.js` (show/hide singleton) + CSS `.app-loading--component`; คง boot overlay ใน `index.html` + เพิ่มคอมเมนต์อธิบายว่ามีสองที่
- [x] `main.js` เรียก `showLoadingOverlay()`/`hideLoadingOverlay()` (ใน `finally`) รอบ route: hub, test-game-hub, game
- [x] เคารพ `prefers-reduced-motion` ทั้งใน CSS และ JS (`renderWithFade`/`dismissPopup` ข้ามการรอ)
- [x] Bump version 0.20.0 → **0.21.0** (package.json + changelog)

---

## 🔗 Related Files
- Transitions (ใหม่): `src/ui/transition/popup-transition.js` (`dismissPopup`), `src/ui/transition/screen-transition.js` (`renderWithFade`)
- Loading (ใหม่): `src/ui/loading-overlay.js` (`showLoadingOverlay`/`hideLoadingOverlay`) — คู่กับ boot overlay ใน `index.html`
- Boot loading: `index.html` (`#app-loading`), `src/main.js` (`finishBootLoading`)
- Routing/screen swap: `src/main.js` (`renderCurrentRoute` — ห่อ `renderWithFade` + `showLoadingOverlay`)
- Popups (ต่อ `dismissPopup`): `src/ui/popup-dialog.js`, `src/ui/game-exit-popup.js`, `src/ui/resting-point-popup.js`, `src/ui/day-completion-popup.js`, `src/ui/checkin-summary-screen.js`
- Styles: `public/style.css` (`.app-popup` enter/leave, `.app-loading--component`, `#ui-root` fade, `.dotted-loader`)
- Related: [US-E7-17](./US-E7-17.md) (Boot Loading โลโก้ + dot progress), [PB-01-01](../problems/PB-01-01.md) (route-race)
- Backlog: [01-product-backlog](../01-product-backlog.md)

---
Back to Product Backlog: [Product Backlog](../01-product-backlog.md) | Back to Index: [Index](../../index.md)
