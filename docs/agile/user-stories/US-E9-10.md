# User Story: US-E9-10 - CLI แก้ไข HN (`update-user-hn.js`)

**Status:** ✅ Done (2026-07-14 — owner verified: CLI [`update-user-hn.js`](../../../update-user-hn.js) — **ไม่ bump version**)
**Epic:** [E9: Field Feedback Hotfixes](../01-product-backlog.md)
**Owner:** [Developer/AI]
**Source:** [Field Feedback — ลงพื้นที่ (2026-07-10)](../meeting-backlogs/2026-07-10.md) §2.11
**Priority:** 🟡 P1 — Committed (Sprint 09)

> **ขอบเขตที่ ship:** เครื่องมือ ops/แอดมิน CLI ที่ root — **ไม่เกี่ยวกับโค้ดเกมในแพ็กเกจ** จึงไม่ bump `package.json` (เทียบเท่า [US-E5-03](US-E5-03.md) + `delete-user.js`)

---

## 📖 Description
**ในฐานะ** แอดมิน/ทีมวิจัย
**ฉันต้องการ** สคริปต์ CLI สำหรับ **แก้ไข ID ผู้เล่น** (ใน DB คือคอลัมน์ `hn`) ที่ลงทะเบียนผิด (เช่น ใส่เบอร์โทรแทน ID)
**เพื่อให้** แก้ข้อมูลได้อย่างปลอดภัยแบบเดียวกับ `delete-user.js` โดยไม่ต้องแก้ DB มือ ๆ

---

## 🐛 Problem (จากการลงพื้นที่ — v1.0.0)
- มีผู้เล่นบางคน **ใส่ ID ผิด** (ใส่เบอร์โทรศัพท์แทน)
- ต้องการเครื่องมือ **update `hn`** ใน database พร้อม cascade ไปตารางที่อ้างอิง
- มี `delete-user.js` เป็นต้นแบบแล้ว — ต้องการ **`update-user-hn.js`** (หรือชื่อเทียบเท่า) ในรูปแบบเดียวกัน

---

## 📝 คำศัพท์ — Frontend vs Backend

| ชั้น | ชื่อที่ใช้ | ตัวอย่างในโค้ด |
| --- | --- | --- |
| **Frontend (UI)** | **ID** / Patient ID | `#patient-id-input`, `patient_login_id`, label `ID{hn}` ในหน้า Sign-up |
| **Backend (DB / CLI)** | **`hn`** | คอลัมน์ `user_data.hn`, FK ใน `user_game_history.hn`, flag CLI `--from-hn` / `--to-hn` |

> ในเอกสารนี้และสคริปต์ CLI ใช้ **`hn`** ตาม schema/database; เวลาอธิบายกับทีมหรือผู้ใช้ UI ให้เข้าใจว่า **ID ที่ผู้เล่นกรอก = `hn` ในระบบหลังบ้าน** เป็นค่าเดียวกัน

---

## ✅ Acceptance Criteria
1. [x] มีสคริปต์ CLI `update-user-hn.js` (หรือชื่อที่สอดคล้อง) ที่ root โปรเจกต์ — pattern เดียวกับ [delete-user.js](../../../delete-user.js)
2. [x] รองรับ: `--from-hn <OLD>` `--to-hn <NEW>` พร้อม `--dry-run` และ `--help`
3. [x] อัปเดต `hn` ใน **`user_data`** และตารางที่ FK อ้างอิง (`user_game_history`, `game_user_log`, `game_replay_log`, `user_game_profile_data` ฯลฯ) **อย่างสอดคล้อง** — atomic ผ่าน `ON UPDATE CASCADE`
4. [x] ตรวจสอบก่อนอัปเดต: `to-hn` **ยังไม่มี** ในระบบ, `from-hn` **มีอยู่**
5. [x] `--dry-run` แสดงแผน + จำนวนแถวที่จะเปลี่ยน โดยไม่เขียน DB
6. [x] ใช้ **service_role key** เท่านั้น (ไม่ใช้ anon key) — เอกสารใน header comment
7. [~] อัปเดต `docs/software/03-data-schema.md` ระบุขั้นตอนแก้ ID/`hn` — mapping Frontend **ID** ↔ Backend **`hn`** มีในหัวข้อ [คำศัพท์](#-คำศัพท์--frontend-vs-backend) ของ story นี้แล้ว; schema doc ยังไม่เพิ่มขั้นตอน CLI

---

## 🛠 Technical Tasks
- [x] สำรวจ FK ทั้งหมดที่อ้างอิง `user_data.hn` — sync กับ `CASCADE_TABLES` ใน delete-user.js
- [x] Implement `update-user-hn.js` — parseArgs, dry-run, validation
- [x] ทดสอบบน staging / copy DB — from ผิด → to ถูก
- [~] เพิ่ม usage ใน README หรือ `docs/wiki/development/` — มีใน header comment + `--help` ของสคริปต์แล้ว

---

## 🔗 Related Files
- Reference: [update-user-hn.js](../../../update-user-hn.js), [delete-user.js](../../../delete-user.js)
- Schema: [03-data-schema](../../software/03-data-schema.md)
- Backlog: [01-product-backlog](../01-product-backlog.md)
- Sprint: [Sprint 09](../sprint-backlogs/sprint-09.md) — committed

---
Back to Product Backlog: [Product Backlog](../01-product-backlog.md) | Back to Index: [Index](../../index.md)
